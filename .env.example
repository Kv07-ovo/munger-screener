# 后端（FastAPI / Starlette）环境变量示例。
# 复制为 .env 并按需修改；.env / *.env 已在 .gitignore，不入库。
# 注意：进程从【环境变量】读取（uvicorn 不会自动加载 .env）。
#   - 本地：可在 shell 里 `export ALLOWED_ORIGINS=...` 再启动 uvicorn；
#   - 线上（Render/Railway/Fly 等）：在平台的环境变量面板设置。

# CORS 允许的前端来源（逗号分隔）。绝不使用 "*"。
# 为空时回退本地默认：http://localhost:5173 与 http://127.0.0.1:5173。
# 线上示例：
#   ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://your-custom-domain.com
ALLOWED_ORIGINS=
